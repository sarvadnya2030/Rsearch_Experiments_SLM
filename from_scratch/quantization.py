"""
Educational weight quantization — placeholder.

Will implement: symmetric int8 quantization of an nn.Linear's weight
(scale = max(|W|) / 127, W_q = round(W / scale)), dequantize, and compare
output error against the full-precision layer on random inputs.

Not implemented yet — placeholder only, out of scope for Experiment 0.
"""
